// Get the DOM elements: the input box and the h2 for output:

// input search event calls checkScrabbleWord function when user hits enter
// search event also occures when user clicks X to clear input box

// inputBox.focus(); // give the input focus (blinking cursor)

function checkScrabbleWord() {

    // clear the output from previous word
    // reset the score from the previous word
    // get the word from the input box 

    // if input box is empty, due to user clicking X to clear it:
        // clear the output to match the no input

    // if inputted word is too long (15 letter max)
        // "Input is too long!";

    // if word is in the Scrabble Dictionary:
   
        // make array of letters from word
        // loop array of letters
            // uppercasee the current letter
            // look up letter value and increment score
        // output Scrabble score for word

    // word is not in the Scrabble Dictionary:
        // 'Input is not in the Scrabble dictionary!';
        
} // end function checkScrabbleWord()
